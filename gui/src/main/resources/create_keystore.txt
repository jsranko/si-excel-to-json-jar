-- Create Keystore .jks
keytool -genkeypair -storetype JKS -alias ssl -keyalg RSA -keysize 2048 -keystore keystore.jks -validity 3650 -dname "CN=Juraj Sranko, OU=IT, O=Sranko Informatik, L=Weingarten, ST=Baden-Württemberg, C=DE"

keytool -export -alias ssl -file key.cer -keystore keystore.jks

keytool -import -v -trustcacerts -alias replserver -file client.cer -keystore truststore.ts

-- Migrate jks -> p12
keytool -importkeystore -srckeystore si.jks -destkeystore si.p12 -deststoretype pkcs12